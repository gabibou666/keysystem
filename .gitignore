node_modules/
.env

# Lockfile racine: la racine n'a aucune dependance (simple delegateur vers server/).
# Le lockfile qui fait foi est server/package-lock.json (npm ci en CI et sur Render).
/package-lock.json
